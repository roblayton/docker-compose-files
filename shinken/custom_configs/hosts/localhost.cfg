define host{
  use                     linux-snmp,generic-host,collectd
  contact_groups          admins
  host_name               localhost
  address                 localhost
  check_command           check_ping
  _SSH_USER               shinken
}
